import { GiShoppingCart } from "react-icons/gi";

const CartWidget = ()=> {
    return (
        <div style= {{
            fontSize: "50px",
        }}>
           < GiShoppingCart />
        </div>
    )
}

export default CartWidget;